base_config: decoupled_base_test.yml
run_name: latency_gtlite_4xa100
base_output_directory: outputs/latency_gtlite
vocab_size: 267
base_num_decoder_layers: 32
base_emb_dim: 1024
base_num_query_heads: 16
base_num_kv_heads: 16
head_dim: 64
base_mlp_dim: 4096
max_target_length: 1024
max_prefill_predict_length: 1024
use_geometric_mixer: true
geom_mixer_kernel_size: 7
geom_curvature_weight: 0.1
geom_curvature_dim: 64
geom_num_triangles: 32
geom_warmup_steps: 5000
steps: 500000
per_device_batch_size: 64
learning_rate: 3.0e-4
learning_rate_schedule: cosine
warmup_steps: 5000
adam_b1: 0.9
adam_b2: 0.95
adam_eps: 1.0e-8
weight_decay: 0.1
dropout_rate: 0.0
position_embedding: rope
enable_checkpointing: true
checkpoint_period: 10000
log_period: 100
eval_interval: 5000
eval_steps: 200
enable_tensorboard: true
dataset_type: grain
grain_file_type: network_arrayrecord
grain_train_files: "data/arrayrecord/train.arrayrecord"
grain_eval_files: "data/arrayrecord/test.arrayrecord"
grain_worker_count: 32
dtype: bfloat16
use_iota_embed: true
remat_policy: minimal_with_context
attention: autoselected
scan_layers: true
hardware: gpu
skip_jax_distributed_system: false
ici_fsdp_parallelism: 4
ici_tensor_parallelism: 1
